// RUN: graphalg-translate --import-graphalg < %s | FileCheck %s

// CHECK-LABEL: @ReduceRows
func ReduceRows(m:Matrix<s, s, int>) -> Matrix<s, s, int> {
    // CHECK: graphalg.pick_any %arg0 : <#dim x #dim x i64>
    return pickAny(m);
}
