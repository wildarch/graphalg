// RUN: graphalg-translate --import-graphalg < %s | FileCheck %s
func Accum(a: Matrix<s, s, int>, b: Matrix<s, s, int>) -> Matrix<s, s, int> {
    // CHECK: %[[#ACCUM:]] = graphalg.ewise %arg0 ADD %arg1
    a += b;
    // CHECK: return %[[#ACCUM]]
    return a;
}
