\documentclass[preview]{standalone}
\usepackage{semantic}
\begin{document}
\[
  \inference[select-unary]{
    F, D, L \vdash e : \langle d_1, d_2, R \rangle \\
    ((\tau_p) \rightarrow \langle 1, 1, \texttt{bool} \rangle) := F[f] \\ 
    \langle 1, 1, R \rangle := \tau_p \\
  }{
    F, D, L \vdash \texttt{select(} f \texttt{, } e \texttt{)} : \langle d_1, d_2, R_2 \rangle
  }
\]

\[
  \inference[select-binary]{
    F, D, L \vdash e_1 : \langle d_1, d_2, R_1 \rangle \\
    F, D, L \vdash e_2 : \langle 1, 1, R_2 \rangle \\
    ((\tau_{p_1} \times \tau_{p_2}) \rightarrow \langle 1, 1, \texttt{bool} \rangle) := F[f] \\ 
    \langle 1, 1, R_1 \rangle := \tau_{p_1} & \langle 1, 1, R_2 \rangle := \tau_{p_2} \\
  }{
    F, D, L \vdash \texttt{select(} f \texttt{, } e_1 \texttt{, } e_2 \texttt{)} : \langle d_1, d_2, R_1 \rangle
  }
\]

% TODO: binary

\end{document}